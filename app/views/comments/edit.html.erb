<div class="comments-form">
  <%= form_for((@comment), :url => suggestion_comments_path(@comment.suggestion)) do |f| %>

  	<%= f.text_field :suggestion_id %><br />

    <%= f.text_area :body, :size => "70x3" %> <br />
   	<%= f.text_field :user_id %>

    <%= f.submit %>
  <% end %>
</div>
